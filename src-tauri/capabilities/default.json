{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main typing classroom window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
